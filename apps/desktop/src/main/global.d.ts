/**
 * Build-time constants injected by electron-vite's `define` (see
 * electron.vite.config.ts). Empty string when the env var is unset —
 * which disables Sentry entirely (local/dev builds).
 */
declare const __EXECUTOR_SENTRY_DSN__: string;
